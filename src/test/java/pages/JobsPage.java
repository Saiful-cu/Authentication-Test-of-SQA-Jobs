package pages;

import org.openqa.selenium.By;
import utils.BasePage;

public class JobsPage extends BasePage {
    public JobsPage(By mainLocator, String pageName) {
        super(mainLocator, pageName);
    }
}
